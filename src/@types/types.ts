export type InputProps = {
  type: string;
  name: string;
  label: string;
  value?: string;
  required?: boolean;
  error?: string;
};

export type ButtonProps = {
  type: string;
  text: string;
  // disabled?: boolean;
};
